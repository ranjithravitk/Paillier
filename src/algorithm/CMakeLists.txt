cmake_minimum_required(VERSION 3.21 FATAL_ERROR)
  find_package(OpenSSL 3.0 REQUIRED)
  



  #target_link_libraries(elgamal_algorithm ${OPENSSL_CRYPTO_LIBRARIES})
  #target_include_directories(elgamal_algorithm PRIVATE ${OPENSSL_INCLUDE_DIR})
  
